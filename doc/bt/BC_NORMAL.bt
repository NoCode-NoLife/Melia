//--------------------------------------
//--- 010 Editor v6.0.2 Binary Template
//
// File: BC_NORMAL.bt
// Author: exec
// Revision:
// Purpose:
//--------------------------------------

short op;
int sequence;
short size;

int type;
if(type == 0x04)
{
    int64 sessionId;
    int barrackMapId; // ? matches id, but doesn't seem to work
    byte bin1[5];
}
